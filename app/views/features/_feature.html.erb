<a href=<%= event_path(feature) %> style="background: url(<%= image_path event.logo %>) no-repeat left center,  url(<%= asset_path 'arrow.png' %>) no-repeat right center;">
<span class="event_name"><%= event_name(event) %> fred</span>

<% features(event).each do |feature| %>
	<p class="event_feature"><%= feature.promotion.name %></p>
<% end %>
<p class="event_times"><%= event_times(event) %></p></a>
